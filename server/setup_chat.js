const { Client } = require('pg'); const client = new Client({ connectionString: 'postgresql://neondb_owner:npg_kJ4fX5MNYZTI@ep-wispy-salad-b3xui4u4-pooler.c-4.ap-southeast-1.aws.neon.tech/neondb?sslmode=require' }); client.connect().then(() => client.query('CREATE TABLE IF NOT EXISTS support_chat_messages (id VARCHAR(64) PRIMARY KEY, ticket_id VARCHAR(64) NOT NULL REFERENCES support_tickets(id) ON DELETE CASCADE, sender_id VARCHAR(64) NOT NULL REFERENCES users(id) ON DELETE CASCADE, text TEXT NOT NULL, is_from_admin BOOLEAN DEFAULT false, created_at BIGINT NOT NULL); CREATE INDEX IF NOT EXISTS idx_support_chat_messages_ticket ON support_chat_messages(ticket_id);')).then(() => { console.log('Created'); client.end(); }).catch(e => { console.error(e); client.end(); });
